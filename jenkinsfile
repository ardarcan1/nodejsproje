pipeline {
    agent {
        docker { 
            image 'nodejsarda:v1.0' // Docker Hub'daki imaj adınız ve tag'ınız
            registryUrl 'https://registry.hub.docker.com' // Docker Registry URL'niz
            registryCredentialsId 'docker-hub-credentials1' // Docker Registry için Jenkins Credentials ID
            reuseNode true
        }
    }

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/ardarcan1/nodejsproje', branch: 'master'
            }
        }

        stage('Install') {
            steps {
                sh 'npm install'
            }
        }

        stage('Determine Version') {
            steps {
                script {
                    // Versiyon numarasını bir dosyadan oku veya varsayılan değeri kullan
                    def version = readFile('version.txt').trim()
                    env.VERSION = "v${version}"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    dockerImage = docker.build("${IMAGE_NAME}:${env.VERSION}")
                }
            }
        }

        stage('Push to DockerHub') {
            steps {
                script {
                    docker.withRegistry('https://registry.hub.docker.com', 'DOCKERHUB_CREDENTIALS') {
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Update Version') {
            steps {
                script {
                    // Versiyon numarasını artır ve dosyaya yaz
                    def version = readFile('version.txt').trim().toInteger() + 1
                    writeFile file: 'version.txt', text: "${version}"
                }
            }
        }
    }
}
